# Domains infrastructure

Create the basic infrastructure that will be used to support the domains of a service environments. It creates a DNS zone, front door and the default DNS records.

## Terraform documentation
For the list of requirement, inputs, outputs, resources... check the [terraform module documentation](tfdocs).
